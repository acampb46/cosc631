<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Purchase Receipt</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .receipt {
            max-width: 600px;
            margin: auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background: #f9f9f9;
        }

        .receipt-header {
            font-size: 1.5rem;
            font-weight: bold;
            text-align: center;
            margin-bottom: 20px;
        }

        .receipt-line {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
        }

        .receipt-footer {
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
<div class="container my-5">
    <div class="receipt">
        <!-- Header -->
        <div class="receipt-header">Purchase Receipt</div>

        <!-- Receipt Details -->
        <div class="receipt-line">
            <span>Purchase ID:</span>
            <span><%= purchase.id %></span>
        </div>
        <div class="receipt-line">
            <span>Item ID:</span>
            <span><%= purchase.item_id %></span>
        </div>
        <div class="receipt-line">
            <span>Price Paid:</span>
            <span>$<%= purchase.price_paid %></span>
        </div>
        <div class="receipt-line">
            <span>Quantity Bought:</span>
            <span><%= purchase.quantity_bought %></span>
        </div>
        <div class="receipt-line">
            <span>Purchase Date:</span>
            <span><%= purchase.purchase_date %></span>
        </div>
    </div>

    <!-- Buy Again Button -->
    <div class="receipt-footer">
        <a href="/assignment4/items/<%= purchase.item_id %>" class="btn btn-primary btn-lg">Buy Again</a>
        <a href="/assignment4/home" class="btn btn-primary btn-lg">Browse All Items</a>
        <a href="/assignment4/dashboard/load" class="btn btn-primary btn-lg">Dashboard</a>
    </div>
</div>

<!-- Bootstrap JS and Dependencies -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
