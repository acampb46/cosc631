<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title><%= pageTitle || 'Home' %></title>
</head>
<body>
<header>
    <h1><%= headerText || 'Welcome to the Auction Site' %></h1>
    <nav>
        <a href="/assignment4/auth/register">Register</a>
        <a href="/assignment4/auth/login">Login</a>
    </nav>
</header>
<main>
    <h2><%= featuredHeading || 'Featured Items' %></h2>
    <div id="item-list">
        <% if (items && items.length > 0) { %>
        <ul>
            <% items.forEach(item => { %>
            <li><%= item.name %> - <%= item.price %></li>
            <% }) %>
        </ul>
        <% } else { %>
        <p>No items available.</p>
        <% } %>
    </div>
</main>
</body>
</html>
